# 09 — Eight-object Versioned Category Exit

Type: task
Status: resolved
Blocked by: 08

Populate all eight fixed Lab slots, run integrated and per-unit certification,
and emit `PASS under versioned category-specific baselines (8/8)` without
creating or implying a complete-island delivery.

## Resolution

The single `/stage-1-5-scene/` delivery now renders all eight Procedural
Replacements in the fixed Eight-slot Lab Reference Layout, using the reference
seven-unit normalization and matching bottom-center slot anchors. It loads no
Authored Reference asset and exposes no isolated object as the final delivery.

`single-mesh-stage-2-eight-object-certification-v1` verifies eight current
versioned positive freezes, eight Chrome/nonvisual acceptances, native Firefox
and Safari hardware-GPU evidence for every object, every declared complex-
appearance damage bracket, and the integrated layout. The result is `PASS under
versioned category-specific baselines (8/8)`. Historical Stage 1 remains `2/4
FAIL`; no full-island result is claimed.

## Certification v2

`single-mesh-stage-2-eight-object-certification-v2` preserves the v1 report and
replaces only the current Candle evidence with its band-limited v3 freeze,
unchanged geometry/semantic acceptance, material scale-consistency gate, damage
controls, and fresh Firefox/Safari evidence. The unified delivery remains 8/8,
asset-free at runtime, and explicitly not a full-island result.
