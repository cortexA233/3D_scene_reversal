# 01 — Umbrella human-anchored patterned-v3

Type: task
Status: resolved

Record the user-approved Umbrella as a hash-frozen positive exemplar and replace
same-position complex-texture hard gates with position-tolerant semantic role
coverage while preserving historical v2 and every geometry/nonvisual gate.

## Answer

`patterned-appearance-baseline-v3` accepts the approved candidate and rejects
flat, wrong-role-palette, flower-deleted, leaf-deleted, branch-deleted, and
half-coverage candidate variants. Exact-position v2 metrics remain diagnostic;
the historical v2 FAIL is unchanged.
