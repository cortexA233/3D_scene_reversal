# 12 — Certify Scene Parity Foundation and record the red baseline

**What to build:** Run the complete Foundation workflow against the actual full-island candidate, prove production isolation and deterministic evidence, publish the reproducible candidate failures and review artifacts, and declare the measuring foundation complete without claiming scene parity.

**Blocked by:** 10 — Calibrate and freeze Scene Quality Baseline v1; 11 — Prove the Reference-guided Fitting Loop.

**Status:** ready-for-agent

- [ ] Add one non-interactive Foundation certification command that distinguishes infrastructure success from the expected current candidate-quality failure.
- [ ] Run Immutable Reference Observation, complete Semantic Coverage, Scene Recipe validation, production generation, all direct 3D evidence, all camera passes, and the frozen Scene Parity Gate Stack in one reproducible workflow.
- [ ] Build and render the replacement-only entrypoint with reference scenes, source, loaders, raw assets, Ground Truth tools, measurement code, fitting code, and acceptance code unavailable to the production import graph.
- [ ] Block external network access with an empty browser profile and prove the candidate requests only approved local runtime resources.
- [ ] Statically reject authored meshes, GLB/GLTF, textures, heightfields, source code, source-node IDs, manifests, base64 payloads, sampled/dense constants, evaluation imports, ambient randomness, and unapproved dependencies.
- [ ] Repeat production generation and normative captures to prove stable Scene Semantic IDs, derived seeds, structure, CPU geometry evidence, bounds, reports, and declared GPU repeatability.
- [ ] Produce a schema-versioned Scene Parity Foundation certification report with `foundation` success independent from the current candidate's expected red gate outcomes.
- [ ] Produce the complete current-candidate red baseline with aggregate and worst-case layout, surface, terrain, coastline, sea-level, horizon, camera-geometry, material, lighting, and appearance evidence.
- [ ] Verify the candidate evaluation command itself fails on the frozen parity gates; Foundation certification may pass only because it expects and records that honest failure rather than overriding it.
- [ ] Generate six-camera contact sheets, overlays, differences, pass previews, and an indexed Human Parity Review package from the same evidence.
- [ ] Rank the next milestone's largest residuals without changing the frozen baseline or beginning unbounded visual fitting.
- [ ] Record the exact branch, Recipe/generator/baseline versions, browser/OS/GPU/Three.js environment, capture moments, and production audit results needed by fresh `/implement` sessions.
- [ ] Re-run the complete relevant test and acceptance suite and finish with a clean experiment worktree while leaving the main worktree untouched.
